class ArticelModel {
  String title;
  String description;
  String url;
  String urlToImage;
  String content;

  ArticelModel(
      {this.title, this.content, this.description, this.url, this.urlToImage});
}
